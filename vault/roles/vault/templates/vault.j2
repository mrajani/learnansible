storage "consul" {
  address = "127.0.0.1:8500"
  path    = "vault"
}

listener "tcp" {
  address     = "0.0.0.0:8200"
  tls_disable = 1
}

seal "awskms" {
  region     = "{{ aws_region }}"
  kms_key_id = "{{ aws_kms_key }}"
}

default_lease_ttl = "15m"
ui = true